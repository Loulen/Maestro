# Node
node_modules/
dist/
*.log

# Rust (will be relevant once Slice 1 lands)
target/
Cargo.lock.*

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Sandcastle local state
.sandcastle/logs/
.sandcastle/worktrees/
.sandcastle/.env

# Maestro runtime state (event log SQLite — never committed)
.maestro/maestro.db
.maestro/maestro.db-journal
.maestro/maestro.db-wal
.maestro/maestro.db-shm

# Maestro per-run worktrees, sub-worktrees, prompts snapshots, artefacts
.maestro/runs/

# Maestro pipeline definitions and library are per-user state — pipelines
# get edited live from the UI, library is per-user. Don't share via git.
.maestro/pipelines/
.maestro/library/

# Playwright artefacts
frontend/test-results/
frontend/playwright-report/
frontend/playwright/.cache/

# Personal agent context — not shared with the team
CLAUDE.md
CLAUDE.local.md
docs/agents/
.claude/

# Env files at any path
.env
.env.local
